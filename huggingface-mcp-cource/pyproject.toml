[project]
name = "huggingface-mcp-cource"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["mcp[cli]>=1.10.1"]

[tool.pyright]
venvPath = "."
venv = ".venv"
